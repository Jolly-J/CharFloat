# P5.2 macOS arm64 发布包核验

构建命令: `npm run dist -- --mac --arm64` → 退出码 0（2026-09-22 10:43:55）

## 产物
```
110347331 release/2.1.0/mac/Office
109360215 release/2.1.0/mac/Office-Agent-Bridge-2.1.0-mac-arm64.zip
```
| 文件 | SHA-256 |
|---|---|
| `Office Agent Bridge-2.1.0-arm64-mac.zip` | `a906ac4a5a4aa4e30cd6e7f2e8167469a48c5d7b3441cc78f118bbace585f1b4` |
| `Office-Agent-Bridge-2.1.0-mac-arm64.zip` | `f272917751a477c38d3aea0a71b8d4942eb9adf7d90d82b5a3a7491399573bfc` |

**命名**：新包使用含版本/平台/架构的命名 `Office-Agent-Bridge-2.1.0-mac-arm64.zip`（DP6 已决议）。旧包 `Office Agent Bridge-2.1.0-arm64-mac.zip` **保留原名未被动**，哈希与构建前一致（`a906ac4a…`），两者并存而非覆盖。

> **产物已更新（2026-09-22 11:49）**：上表 `f2729177…`（10:43 构建）之后，为权限引导浮窗修复又重打了一次包，同名文件被覆盖。**当前有效产物**：
>
> | 文件 | 大小 | SHA-256 |
> |---|---|---|
> | `Office-Agent-Bridge-2.1.0-mac-arm64.zip` | 109,370,175 | `c3579fdb6cb5b3f5252aa70553faa6566c73b5319678b84e5f15a24c2bf6c8d1` |

## 使用者验收（2026-09-22）

使用者明确 **mac 与 win 构建物均验收通过**（原话："可以，win运行正常，mac和win构建物我都验收"，Windows 侧记录见 [p5.2-windows-cross-build.md](p5.2-windows-cross-build.md)）。

- 验收层级：**构建物验收**——使用者接受上述 mac arm64 zip 作为交付物。
- **未据此认定**：打包后 `.app` 是否启动过、真实宿主业务矩阵、加载项安装/升级。这些仍按下方"未做"清单登记。

## 架构与签名
```
主程序 lipo -archs        : arm64
Electron Framework arch   : arm64
CodeDirectory flags       : 0x10000(runtime) —— 启用 Hardened Runtime
Authority                 : Apple Development: 8374606771 (3UMWX9NWJ3)
TeamIdentifier            : U7PRG5WNR7
spctl -a -vv              : rejected（origin=Apple Development）
```
**结论**：内部运行验收可用；**正式分发受阻**——当前签名是 Apple Development 证书而非 Developer ID Application，Gatekeeper 拒绝、无法公证。这与 DP2「内部运行验收与正式分发要求分开记录」一致：需要凭据时再处理。

## 包内资源
```
Contents/Resources/app.asar                              46M
Contents/Resources/app.asar.unpacked/dist/bridge/cli.cjs 1.8M
app.asar.unpacked/ 下: dist  office-addon  resources  skills  wps-addon
wps-addon/      : addon-core.js index.html logo.png manifest.xml ribbon.xml status.html table-format.png
office-addon/   : excel/ public/
skills SKILL.md : 4 个（office-agent-bridge、-chart-style、-ppt-design、-word-batch-edit）
```
- **开发规范未入包**：包内无任何 `AGENTS.md`（`!**/AGENTS.md` 生效）。
- **加载项源码未入包**：包内无 `*/src/**`（`!wps-addon/src/**`、`!office-addon/src/**` 生效），只带部署入口。
- **用户 skill 资源完整**：4 个 SKILL.md 均在包内，安装器可用。

## P5.3 从实际发布包运行（非源码、非 dist）
```
$ node <包内>/app.asar.unpacked/dist/bridge/cli.cjs --help        → 输出版本帮助，退出码 0
$ node <包内>/cli.cjs --serve 然后 GET /health                    → {"service":"wps-bridge","protocol":2,"version":"2.1.0"}
GET /api/v1/mcp-tools（带 token）                                 → 91 个工具
GET /api/v1/prompts/aesthetic（带 token）                         → HTTP 200
```
包内 `office-addon/excel/manifest.xml` 存在，安装器资源路径正确。

**日志中一条需说明的无害提示**：`本地 HTTPS 服务启动跳过: listen EADDRINUSE 127.0.0.1:19891`——测试时使用者的 `npm run dev` 实例正占用 19891，属预期的双客户端共存行为，HTTP 主端口正常监听。

## 未做（不得视为已通过）
- **macOS 侧未启动打包后的 GUI**（Electron 主界面/托盘）——本机只从包内运行了 CLI 与后台服务，`npm run dev` 的开发态另算。
- **未做真实宿主业务验收**（P5.4）：未在真实 WPS/Excel 中执行业务矩阵，未做保存后重开核验。
- **未做加载项安装/升级验收**（P5.5）。
- Windows 侧的打包、exe 图标修复与实机运行已完成，记录在 [p5.2-windows-cross-build.md](p5.2-windows-cross-build.md)；Windows 侧的**业务矩阵**同样未做。
