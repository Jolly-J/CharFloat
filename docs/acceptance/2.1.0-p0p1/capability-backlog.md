# 能力补强专项台账（Capability Backlog）

建立时间：2026-09-22 · 状态：**待排期**（本轮先修缺陷，专项后期做）

**这份文件回答一个问题：AI 现在还能多做什么。** 与 [issues.md](issues.md)（缺陷台账）分开维护：
- `issues.md` = **现有能力做错了**（假成功、静默失败、数据破坏）→ 修
- 本文件 = **宿主能做的、我们还没让 AI 用上**（缺口）→ 补

**依据来源**：[08-ms-vs-wps.md](p5/mcp-sweep/08-ms-vs-wps.md)（MS vs WPS 能力对照，552 行）、[07-tool-gap.md](p5/mcp-sweep/07-tool-gap.md)（工具面缺口，541 行）、[06-wps-api-map.md](p5/mcp-sweep/06-wps-api-map.md)（WPS 对象模型成员清单）。

## 依据等级口径（务必区分，不要当成已实测）

| 等级 | 含义 |
|---|---|
| **源码可证** | 我们自己的代码里能直接读出（分支存在/不存在） |
| **文档可证** | 官方 API 文档写了，但**本机未实测** |
| **推测** | 从 VBA/COM 经验推断，**未验证** |
| **已实测** | 在本机真实宿主上跑过并有读数 |

> ⚠️ 大部分"宿主支持"目前是**文档可证**而非已实测——补的时候第一步就是**实测确认**，不要直接照文档开写。

---

## A 类：全新能力（宿主能做，MCP 完全没有）

| 编号 | 能力 | 宿主支持 | 依据 | 补在哪一侧 | 价值 | 状态 |
|---|---|---|---|---|---|---|
| **CAP-01** | **Excel 分页与打印设置**：打印区域、打印标题行列、纸张、方向、页边距、页眉页脚、网格线/批注打印、手动分页符 | 两边都支持（Office.js `PageLayout`/`PageBreakCollection`；WPS `PageSetup`） | 文档可证 | 两侧 | AI 做不出"可直接打印装订"的报表 | ✅ 已完成（真机验证：打印区域/标题行/横向/A4/居中/页眉全部读回一致） |
| **CAP-02** | **Excel 导出 PDF/XPS** | WPS `ExportAsFixedFormat`；Office.js 不支持（需 COM/JXA） | 文档可证 | WPS 侧 | 交付链路断在最后一步 | ✅ 已完成（真机验证：工作表 PDF 441KB、整册 PDF 1.79MB，桥接侧确认落盘） |
| **CAP-03** | **单元格内局部格式（富文本）**：只把一句话里某段加粗/变红/改字号 | WPS `Range.Characters`；Office.js 需整格或 OOXML | 文档可证（WPS） | WPS 侧优先 | **"精细操控文档"的典型诉求**，目前只能整格统一 | ✅ 已完成（真机验证 12/12：片段加粗变红、独立改字号互不影响） |
| **CAP-04** | **Word 域更新与交叉引用**：目录页码、交叉引用、页数随内容刷新 | 两边都支持 | 文档可证 | 两侧 | 现在只"插入目录"，**改一下页码就全错** | ✅ 已完成（真机验证：目录页码 [3×6,4×3,5×5] → [4×6,5×3,6×5]） |
| **CAP-05** | **Word 内容控件全类型**：日期、复选框、下拉、组合框、富文本、纯文本（含 `listItems`） | WordApi 1.9 / VBA 全类型 | 文档可证 | 两侧 | AI 做不出"可填模板/合同填空位/表单" | ✅ 已完成（真机验证：6 类控件可建；group/repeatingSection 如实报宿主未实现） |
| **CAP-06** | **条件格式图标集与完整规则族**：红黄绿灯、Top10、重复值、文本包含、公式规则、规则列表与更新 | 两边都支持（WPS `AddIconSetCondition`/`Top10`；Office.js 1.17） | 文档可证 | 两侧 | 报表缺"红黄绿灯"这类最常见的业务信号 | ✅ 已完成（真机验证：图标集/Top-N/重复值/公式/文字包含 全部通过） |
| **CAP-07** | **WPS 表格侧矢量绘图与 SmartArt**：`AddShape`/`AddTextEffect`/`AddSmartArt`/`BuildFreeform`/`ThreeD`/`Shadow`/格式刷 | WPS 宿主对象模型 | 文档可证 | WPS 侧 | WPS 明明能画，我们只会 `add_chart` | ✅ 已完成（真机 11/11：几何/直线/文本框/**艺术字**/**分组解组**/层级/改删/读回全部通过；WPS 比 MS Excel 更能画） |
| **CAP-08** | **MS 侧矢量绘图**：几何形状、连接符、SVG、文本框、分组、层级、旋转缩放、形状导图 | Office.js ExcelApi 1.9 `Shape` 全套 | 文档可证 | Office.js 侧 | MS 用户完全用不上那套 134 元素信息图 | ✅ 部分完成（真机：矩形/文本框/层级/导图/缩放/删除读回可用；**直线报宿主限制、SVG 与 getActiveShape 本机无 API、组合经工具未打通**） |
| **CAP-09** | **PPT 页面尺寸与母版/版式控制、批量导出** | MS `SlideMaster`/`SlideLayout`（**无页面尺寸、无导出**）；WPS 有 `PageSetup` | 文档可证 | 两侧 | 没法把 4:3 改 16:9、没法套公司母版、没法批量导图 | 待排期 |
| **CAP-10** | **数据验证读回与违规定位** | MS `getInvalidCells`；WPS 读 `Validation` 属性 | 文档可证 / 推测 | 两侧 | AI 写完下拉/范围校验，**无法自检哪些值越界** | 待排期 |
| **CAP-11** | **事件驱动的"变更感知"**：用户刚改了哪个单元格、保存前拦一手 | WPS 45+ Application 事件 + `wps.ApiEvent`；Office.js 仅 3 个事件 | 文档可证 | WPS 侧 | AI 只能盲写，无法基于用户刚做的修改做增量协作 | 待排期 |
| **CAP-12** | **Excel 线程化批注完整语义**：回复、@提及、标记已解决 | Office.js `Comment` | 文档可证 | Office.js 侧 | AI 只能"贴一条"，不能参与讨论 | 待排期 |
| **CAP-13** | **数据透视表字段编排与刷新** | Office.js `PivotTable` 可加行/列/数据字段；WPS `PivotFields` | 文档可证 | 两侧 | MS 侧"建透视表"目前只能建空壳 | 待排期 |
| **CAP-14** | **Excel 切片器 / 工作表视图 / 链接数据类型（富值）** | MS 支持；WPS **未查证** | 文档可证 / 未查证 | Office.js 侧 | 交互式仪表盘、"从数据源自动带出公司/股票信息" | 待排期 |
| **CAP-15** | **区域复制（`copy_range`）** | 宿主支持 | 源码可证（我方未实现） | WPS 侧补齐 | MS 有、WPS 没有 | 待排期 |
| **CAP-16** | **超链接（`set_hyperlink`）** | 宿主支持 | 源码可证 | WPS 侧补齐 | 同上 | 待排期 |
| **CAP-17** | **命名区域**（增删查改） | WPS 宿主支持 | 文档可证 | WPS 侧补齐 | 同上 | 待排期 |
| **CAP-18** | **文档属性**（Title/Subject/Author/Keywords 等 8 字段 + 自定义属性） | WPS 宿主支持 | 文档可证 | WPS 侧补齐 | 同上 | 待排期 |
| **CAP-19** | **结构化表格 Table**（创建/更新/样式/汇总行/resize） | WPS 有 ListObject（文档可证）；Office.js 有 `tables` | 文档可证 | WPS 侧补齐 | 同上 | 待排期 |
| **CAP-20** | **Excel 图片与形状管理**（插入图片、列出/移动/删除形状） | 宿主支持 | 源码可证（WPS 表格侧为零） | WPS 侧补齐 | 同上 | 待排期 |
| **CAP-21** | **图表更新（`update_chart`）** | WPS 加载项缺该 RPC 分支（已知缺口） | 源码可证 | WPS 侧 | 唯一被声明的宿主缺口 | 待排期 |
| **CAP-22** | **图表图片导出（`export_chart_image`）** | Office.js `chart.getImage()`；WPS 只有整表截图 | 源码可证 | WPS 侧补齐 | 单图导出 | 待排期 |
| **CAP-23** | **工作表保护 / 标签色** | MS 侧有 `protect`/`tabColor`；WPS **未实现且 schema 不含** | 源码可证 | 两侧 | 双向不通 | 待排期 |
| **CAP-24** | **Word 关键词加粗（按 `searchQueries`）** | **宿主已实现**，schema 未暴露 | 源码可证 | 只差工具定义 | 零成本 | 待排期 |

## B 类：可验证性补强（能写不能读 → AI 无法自检）

> 这类不是"新玩法"，但**没有它 AI 就不敢动手**：写完看不见结果，只能盲信 `success`。

| 编号 | 能力 | 现状 | 状态 |
|---|---|---|---|
| **CAP-30** | **条件格式读回** | 能加，读不回 | ✅ 已完成已验证（真机：读回规则类型/运算符/公式/填充色/字色/优先级） |
| **CAP-31** | **冻结窗格读回** | 能设，读不回 | ✅ 已完成已验证（真机：读回 frozen/freezeRowIndex/freezeColumnIndex + 原始 SplitRow/SplitColumn） |
| **CAP-32** | **数据有效性读回** | 能设，读不回 | ✅ 已完成已验证（真机：读回类型/候选项公式/下拉/错误与提示文案） |
| **CAP-33** | **筛选状态读回** | 能设，读不回 | ◐ 读回已完成已验证（真机读回筛选范围与各列条件）；**暴露写入侧既有缺陷 ISS-117** |
| **CAP-34** | **工作表保护 / 标签色读回** | 能设，读不回 | ✅ 已完成已验证（真机：读回 protectContents/ProtectDrawingObjects/ProtectionMode + 标签色） |
| **CAP-35** | **Word 页眉页脚 / 水印读回** | 能设，读不回 | ✅ 已完成已验证（真机：读回 2 节页眉页脚 + 文档级奇偶/首页开关 + 水印探测） |
| **CAP-36** | **透视表读回** | 能建，读不回，且建表瞬间是空骨架 | ◐ 读回已完成（读回名称/数据源/字段布局/刷新时间）；**暴露写入侧既有缺陷 ISS-118** |

## C 类：零成本死分支（宿主已实现 + 网关已注册，只差 schema）

| 编号 | 工具 | 说明 | 状态 |
|---|---|---|---|
| **CAP-40** | `clear_range` | 清空区域。现在只能写 `null` 矩阵，极易残留 | 本轮在做（ISS-45） |
| **CAP-41** | `get_style_token` | 读原表设计语言（主题/样式令牌） | 本轮在做（ISS-45） |
| **CAP-42** | `word_capture_preview` | Word 页面预览。**实现后 Word 才有视觉验收通路** | 本轮在做（ISS-71/95） |

## D 类：契约与一致性补强（跨宿主）

| 编号 | 内容 | 依据 | 状态 |
|---|---|---|---|
| **CAP-50** | 修 **M1–M8 跨宿主字段错位**：`set_data_validation`（先清校验再什么都不设）、`find_and_replace`（空串搜索→全局替换）、`manage_sheet` 4 个 action 静默 no-op、`get_charts` 忽略选择器、`add_chart` 忽略 5 参数、`manage_rows_and_columns` 想插列却插行 | 源码可证 | 本轮在做（ISS-93） |
| **CAP-51** | COM 回退白名单按**实际覆盖**生成（现在把整张 30 项路由表当白名单，COM 实际只覆盖 28/30） | 源码可证 | 本轮在做（ISS-97） |
| **CAP-52** | MS 侧**矢量绘图域**整体补齐（与 CAP-08 同源） | 文档可证 | 待排期 |

---

## 建议的排期顺序（按"投入产出比"）

| 批次 | 内容 | 理由 |
|---|---|---|
| **第 1 批** | CAP-40/41/42（零成本死分支）+ CAP-30~36（读回） | 成本最低、立刻让 AI"敢动手"；且是补能力的**基础设施** |
| **第 2 批** | CAP-03（富文本）+ CAP-04（域更新）+ CAP-06（图标集） | 都是"精细操控"的高频诉求，价值/成本比最高 |
| **第 3 批** | CAP-01（打印）+ CAP-02（导出 PDF）+ CAP-15~22（WPS 侧 7 项独有能力） | 补齐 WPS 与 MS 的能力差集，交付链路完整 |
| **第 4 批** | CAP-05（内容控件）+ CAP-07（WPS 矢量绘图）+ CAP-08（MS 矢量绘图） | 单条工作量较大，价值高但要独立验证 |
| **第 5 批** | CAP-09~14、CAP-23/24 | 长尾与需查证项 |

## 每个能力补齐时的"完成定义"（统一口径）

补一条能力必须同时交付，缺一不算完成：

1. **宿主实现**：`wps-addon/src/**` 或 `office-addon/src/**` 里的真实实现；
2. **工具定义**：`src/bridge/tools/definitions/**` 的 schema 与说明（写明宿主限制与参数语义）；
3. **读回能力**：有写就必须有读，否则 AI 无法自检；
4. **契约快照**：跑 `npx tsx scripts/snapshot-tools.ts` 记录差异，说明新增/变更了哪些工具；
5. **skill 说明**：`skills/office-agent-bridge/**` 里写清怎么用、有什么坑；
6. **真机验证**：在真实 WPS / Microsoft Excel 上跑出**读数**（不是"构建通过"），并把证据留档到 `docs/acceptance/`；
7. **回归**：`typecheck 0`、`npm test` 全过、`check:claims` / `check:agents` 0。

## 维护约定

- 每条补完后把"状态"改成 `已完成（日期 + 证据路径）`，**不删条目**；
- 实测发现宿主其实不支持的，状态改 `不补（宿主不支持 + 证据）`；
- 新增能力缺口一律先登记到本文件，再排期。
